# Cube Events Labels

Printable event-label tool for The Cube.

## Netlify setup

Use this repo as the source and set:

- Base directory: `cube-events`
- Build command: `npm run build`
- Publish directory: `dist`

The app will then build from this folder as its own site.
